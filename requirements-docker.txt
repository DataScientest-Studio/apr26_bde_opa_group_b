# Runtime dependencies for the Dockerized services ONLY.
# This is intentionally slim — it leaves out Jupyter/notebook/dev tooling that
# lives in the full requirements.txt, so the container image stays small.
# Versions are pinned to match the project's tested environment.

# --- Web API ---
fastapi==0.136.3
uvicorn==0.49.0
websockets==16.0

# --- Databases ---
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12      # binary build: no compiler/libpq needed in the image
pymongo==4.17.0
dnspython==2.8.0

# --- Data & ML ---
pandas==3.0.3
numpy==2.4.4
scikit-learn==1.9.0
joblib==1.5.3

# --- Clients / config ---
requests==2.34.0
websocket-client==1.9.0
python-dotenv==1.2.2

# --- Dashboard ---
streamlit==1.58.0
plotly==6.8.0
